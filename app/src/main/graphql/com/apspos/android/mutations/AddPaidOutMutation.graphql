mutation AddPaidOut ($attributes: PaidOutInput!) {
    addPaidOut(attributes: $attributes) {
        success
        message
        errors {
            message
            path
        }
        id
    }
}
