mutation ClockInMutation ($pin: String!, $storeId: String!) {
    clockIn(pin: $pin, storeId: $storeId) {
        success
        message
        isClocking
        totalHours
        clockIn
        lastClockOut
        error
        shiftId
        startedDay
    }
}