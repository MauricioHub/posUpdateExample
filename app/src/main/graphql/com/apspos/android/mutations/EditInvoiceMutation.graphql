mutation EditInvoiceMutation ($attributes: InvoiceInput!, $id: ID!) {
    editInvoice(attributes: $attributes, id: $id) {
        success
        message
        errors {
            message
            path
        }
    }
}