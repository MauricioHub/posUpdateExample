query EmployeeTipsDetailQuery ($date_start: String, $store_id: String!, $date_end: String, $shift_id: String) {
    employeeTipsDetailReports(dateStart: $date_start, dateEnd: $date_end, shiftId: $shift_id, storeId: $store_id) {
        name
        quantity
        total
    }
}