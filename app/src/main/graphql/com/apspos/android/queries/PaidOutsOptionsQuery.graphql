query PaidOutsOptionsQuery($storeId: String!) {
    paidOutOptions(storeId: $storeId) {
        id
        name
        managerApproval
        openCashDrawer
        requireInvoice
        requireContact
        requireComment
    }
}